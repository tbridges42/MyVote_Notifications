Firefox must be installed
Will not currently work without Python 3.5 installed
Must run 'pip install selenium'

Must have config.txt with username and password, on separate lines, as 'username=name' without svrs

Compare mailing addresses and eliminate as necessary

use smtplib to send email